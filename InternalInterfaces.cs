using Nickel;

namespace Brandenfascher.TrentonChar;

internal interface TrentonCard
{
    static abstract void Register(IModHelper helper);
}

internal interface TrentonArtifact
{
    static abstract void Register(IModHelper helper);
}
